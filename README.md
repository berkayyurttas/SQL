# 📊 SQL Çalışmalarım

Bu depo, temelden ileri seviyeye kadar yazdığım SQL sorgularını ve veri tabanı yönetim notlarımı içermektedir.

## 📁 İçerik Özeti
- **Temel Sorgular:** `SELECT`, `WHERE`, `ORDER BY`, `BETWEEN` kullanımları.
- **Veri Manipülasyonu:** `INSERT`, `UPDATE`, `DELETE` işlemleri.
- **Tablo Yönetimi:** `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`.
- **Gelişmiş İşlemler:** - `GROUP BY` ve `COUNT`/`SUM` gibi agregat fonksiyonlar.
  - **Joins:** `INNER JOIN`, `LEFT JOIN` ve `RIGHT JOIN` ile tablo birleştirme alıştırmaları.

## 🛠 Kullanılan Araçlar
* Microsoft SQL Server (MSSQL)
* SQL Query Shortcuts & Scripts
